# Release Evidence Records

Committed release evidence can live here when a release needs a durable record
inside the repository.

Use this filename pattern:

```text
vX.Y.Z.md
```

Start from [../release-evidence-template.md](../release-evidence-template.md).
Do not commit private hostnames, credentials, private paths, or unnecessary USB
serial details.
