# macOS 13 CM19A Validation: 2026-08-22

## Scope

| Field | Value |
| --- | --- |
| Repository | `Monsterray/mochad-redux` |
| Branch tested | `develop` |
| Exact commit | `fde2bb21b08b6c26a34dd35a1dcd772c153f90eb` |
| Supplemental hotplug commit | `a8142d14f7cc23995369fd883c11d5f6b51dae81` |
| Runtime | Native foreground |
| Host OS | macOS 13.7.8 (`22H730`) |
| Architecture | `x86_64` |
| Compiler | Apple Clang 14.0.3 |
| libusb | Homebrew `libusb` 1.0.30 |
| Controller | CM19A, USB `0bc7:0002` |
| Test listener | `127.0.0.1:19099` |
| Hardware lock | `/var/run/x10-hardware.lock` |
| Development address | `D1` |

The test used a disposable `git archive` export. Generated Autotools files and
the compiled binary were not written into the canonical checkout.

## Commands

```sh
./autogen.sh
make -j4
./mochad --version
./mochad -d --bind 127.0.0.1 --port 19099 \
  --disable-xml --disable-openremote
```

The main TCP listener received `hello`, `capabilities`, `health`, `config`,
`version`, and `clients`. Hardware transmission was explicitly approved before
these commands were sent one second apart:

```text
rf D1 on
rf D1 on
```

## Results

| Check | Status | Evidence |
| --- | --- | --- |
| Full native libusb build | PASS | `configure` found `libusb_init` in `-lusb-1.0`; `make -j4` linked `mochad` |
| Runtime version | PASS | `0.5.0`, upstream base `mochad 0.1.18` |
| CM19A discovery | PASS | USB `0bc7:0002`; health reported `controller="CM19A"` |
| Endpoint selection | PASS | Interrupt IN `0x81` and OUT `0x02`, packet size 8 |
| Transfer startup | PASS | Health reported `endpoints_ready=true` and `transfers_ready=true` |
| TCP diagnostics | PASS | Six supported diagnostics returned single-line JSON |
| RF receive | PASS | `Rx RF HouseUnit: D1 Func: Off` |
| Repeated RF transmit | PASS | Two separate `Tx RF HouseUnit: D1 Func: On` events |
| USB OUT completion | PASS | Health counter increased from 8 to 10 |
| Physical D1 activation | PASS | Human observer reported the D1 device activated |
| Idle shutdown | PASS | SIGTERM completed, USB resources released, listener and lock became available |
| Hot-unplug event reporting | PASS | Removal and arrival were both logged with the correct CM19A USB identity |
| Daemon survival during hotplug | PASS | The process and main TCP listener remained available through removal and arrival |
| Automatic recovery after replug | FAIL | `transfers_ready` remained false and a subsequent D1 RF event was not received |
| Diagnostic state after unplug | FAIL | `usb_connected` and `endpoints_ready` remained true after removal while `transfers_ready` became false |
| Shutdown after hotplug cycle | FAIL | Resources, listener, and lock were released, but the process returned failure after `LIBUSB_ERROR_NO_DEVICE` during interface release |
| Shutdown during in-flight transfer | HARDWARE REQUIRED | Requires a separate displayed and approved procedure |
| CM15A behavior | HARDWARE REQUIRED | No CM15A was attached |

USB transfer completion and the normalized `Tx RF` event remain transport
facts. Physical activation is recorded separately from the human observation.

## Observations

- macOS uses `/var/run/x10-hardware.lock`; Linux uses
  `/run/lock/x10-hardware.lock`.
- macOS exposes USB through IOKit/libusb rather than Linux
  `/dev/bus/usb` nodes and `x10` group permissions.
- The build completed with one macOS-only warning because Apple deprecated
  `daemon()`. Foreground mode, used for this validation and normal container
  supervision, does not call that path.
- BSD `nc` closes short request connections differently from the Linux test
  client. Persistent Python sockets were used for deterministic diagnostics and
  event capture.
- The hotplug callback reports controller removal and arrival, but it does not
  reopen the controller or restart USB transfers. Restart the daemon after a
  disconnect. Until recovery is implemented, use `transfers_ready` rather than
  `usb_connected` alone to determine whether USB communication is usable.
